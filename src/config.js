
const config = {
  n: 1,
  speed: 0.2,
  rot: 2 / 180 * Math.PI,
  viewport: {
    width: 1200,
    height: 690
  },
  fieldOfView: {
    range: 100,
    theta: 120 / 180 * Math.PI
  },
  framerate: Math.floor(1000 / 60), // (ms)
}

export default config