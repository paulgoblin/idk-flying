
const config = {
  n: 50,
  speed: 0.3,
  rot: 4 / 180 * Math.PI,
  viewport: {
    width: 800,
    height: 500
  },
  fieldOfView: {
    range: 50,
    theta: 90 / 180 * Math.PI
  },
  framerate: 1000 / 60, // (ms)
}

export default config